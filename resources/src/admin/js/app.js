'use strict';

angular.module('WPGCMS', [
        'ui.router',
        'wpg.all',
        'wpg.config'
    ])
    .run(function($rootScope, $window, $location, $timeout, $state, $http, wpgPopup, wpgNotify) {

    });